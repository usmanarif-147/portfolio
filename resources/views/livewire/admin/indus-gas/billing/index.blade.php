<div><div class="flex justify-between mb-8"><div><h1 class="text-2xl font-mono font-bold text-white uppercase">Payments & Ledgers</h1><p class="text-gray-500 mt-1">Generate invoices and record customer collections.</p></div><a wire:navigate href="{{ route('admin.indus-gas.payments.create') }}" class="bg-primary text-white rounded-lg px-4 py-2.5 text-sm">Record Payment</a></div>@if(session('success'))<div class="mb-5 bg-emerald-500/10 text-emerald-300 rounded-lg p-3">{{session('success')}}</div>@endif<div class="grid lg:grid-cols-2 gap-6"><div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><div class="p-5 border-b border-dark-700"><h2 class="text-white">Deliveries Awaiting Invoice</h2></div>@forelse($pendingDeliveries as $delivery)<div class="p-4 border-b border-dark-700 flex justify-between"><div><p class="text-gray-200">{{ $delivery->customer->title }}</p><p class="text-gray-500 text-xs">{{ $delivery->delivery_date->format('d M Y') }}</p></div><button wire:click="generateInvoice({{ $delivery->id }})" class="text-primary-light text-sm">Generate Invoice</button></div>@empty<p class="p-5 text-gray-500">No pending deliveries.</p>@endforelse</div><div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><div class="p-5 border-b border-dark-700"><h2 class="text-white">Invoices</h2></div>@forelse($invoices as $invoice)<div class="p-4 border-b border-dark-700 flex justify-between"><div><a wire:navigate href="{{ route('admin.indus-gas.ledgers.show',$invoice->customer) }}" class="text-gray-200">{{ $invoice->customer->title }}</a><p class="text-gray-500 text-xs">{{ $invoice->invoice_number }} · Rs. {{ number_format($invoice->total_amount) }}</p></div><a href="{{ route('admin.indus-gas.invoices.pdf',$invoice) }}" class="text-primary-light text-sm">PDF</a></div>@empty<p class="p-5 text-gray-500">No invoices yet.</p>@endforelse</div></div></div>
