<div><div class="flex justify-between mb-8"><div><h1 class="text-2xl font-mono font-bold text-white uppercase">{{ $customer->title }} Ledger</h1><p class="text-gray-500 mt-1">Running balance from invoices and payments.</p></div><a href="{{route('admin.indus-gas.ledgers.pdf',$customer)}}" class="bg-primary text-white rounded-lg px-4 py-2.5 text-sm">Download PDF</a></div><div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><table class="w-full"><thead><tr class="bg-dark-700/50"><th class="text-left p-4 text-xs text-gray-400">DATE</th><th class="text-left p-4 text-xs text-gray-400">TYPE</th><th class="text-left p-4 text-xs text-gray-400">REFERENCE</th><th class="text-right p-4 text-xs text-gray-400">DEBIT</th><th class="text-right p-4 text-xs text-gray-400">CREDIT</th><th class="text-right p-4 text-xs text-gray-400">BALANCE</th></tr></thead><tbody>@php($balance=0)@forelse($entries as $entry)@php($balance += $entry['debit']-$entry['credit'])<tr class="border-t border-dark-700"><td class="p-4 text-gray-300">{{ $entry['date']->format('d M Y') }}</td><td class="p-4 text-gray-300">{{ $entry['type'] }}</td><td class="p-4 text-gray-400">{{ $entry['number'] }}</td><td class="p-4 text-right text-gray-200">{{ $entry['debit'] ? number_format($entry['debit']) : '—' }}</td><td class="p-4 text-right text-emerald-300">{{ $entry['credit'] ? number_format($entry['credit']) : '—' }}</td><td class="p-4 text-right text-white font-mono">{{ number_format($balance) }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">No invoice or payment entries yet.</td></tr>@endforelse</tbody></table></div></div>
