<div>
    <div class="flex items-center gap-2 text-xs text-gray-500 mb-2"><a href="{{ route('admin.indus-gas.settings.index') }}" wire:navigate class="hover:text-gray-300">Indus Gas Settings</a><span>›</span><span class="text-gray-300">Suppliers</span></div>
    <div class="flex items-center justify-between mb-8"><div><h1 class="text-2xl font-mono font-bold text-white uppercase tracking-wider">Suppliers</h1><p class="text-gray-500 mt-1">Manage LPG, cylinder, and future suppliers.</p></div><a href="{{ route('admin.indus-gas.settings.suppliers.create') }}" wire:navigate class="bg-primary hover:bg-primary-hover text-white font-medium rounded-lg px-4 py-2.5 text-sm">Add Supplier</a></div>
    @if (session('success'))<div class="mb-4 bg-emerald-500/10 border border-emerald-500/30 text-emerald-300 rounded-lg px-4 py-3 text-sm">{{ session('success') }}</div>@endif
    <div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><div class="overflow-x-auto"><table class="w-full"><thead><tr class="bg-dark-700/50"><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Supplier</th><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Supplies</th><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Contact</th><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Status</th><th class="text-right px-6 py-3 text-xs text-gray-400 uppercase">Actions</th></tr></thead><tbody class="divide-y divide-dark-700">@forelse($suppliers as $supplier)<tr><td class="px-6 py-4 text-sm text-white font-medium">{{ $supplier->name }}</td><td class="px-6 py-4 text-sm text-gray-400">{{ ucfirst($supplier->supply_type) }}</td><td class="px-6 py-4 text-sm text-gray-400">{{ $supplier->contact_person ?: '—' }} @if($supplier->phone)<span class="block text-xs">{{ $supplier->phone }}</span>@endif</td><td class="px-6 py-4"><span class="text-xs {{ $supplier->is_active ? 'text-emerald-400' : 'text-gray-500' }}">{{ $supplier->is_active ? 'Active' : 'Inactive' }}</span></td><td class="px-6 py-4 text-right"><a href="{{ route('admin.indus-gas.settings.suppliers.edit', $supplier) }}" wire:navigate class="text-primary-light text-sm">Edit</a><x-admin.confirm-button title="Delete supplier?" text="Delete only if it has not been used in a transaction." action="$wire.delete({{ $supplier->id }})" confirm-text="Delete" class="text-red-400 text-sm ml-3">Delete</x-admin.confirm-button></td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-gray-500">No suppliers yet. <a class="text-primary-light hover:underline" wire:navigate href="{{ route('admin.indus-gas.settings.suppliers.create') }}">Add BBN Plant or FAS Tube</a>.</td></tr>@endforelse</tbody></table></div></div>
</div>
