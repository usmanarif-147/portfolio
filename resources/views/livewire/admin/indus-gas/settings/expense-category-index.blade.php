<div><div class="flex items-center gap-2 text-xs text-gray-500 mb-2"><a href="{{ route('admin.indus-gas.settings.index') }}" wire:navigate class="hover:text-gray-300">Indus Gas Settings</a><span>›</span><span class="text-gray-300">Expense Categories</span></div><div class="flex items-center justify-between mb-8"><div><h1 class="text-2xl font-mono font-bold text-white uppercase">Expense Categories</h1><p class="text-gray-500 mt-1">Create the categories you will use for daily expense records.</p></div><a href="{{ route('admin.indus-gas.settings.expense-categories.create') }}" wire:navigate class="bg-primary text-white rounded-lg px-4 py-2.5 text-sm">Add Category</a></div>@if(session('success'))<div class="mb-4 bg-emerald-500/10 border border-emerald-500/30 text-emerald-300 rounded-lg px-4 py-3 text-sm">{{ session('success') }}</div>@endif<div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><table class="w-full"><thead><tr class="bg-dark-700/50"><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Category</th><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Description</th><th class="text-left px-6 py-3 text-xs text-gray-400 uppercase">Status</th><th class="text-right px-6 py-3 text-xs text-gray-400 uppercase">Actions</th></tr></thead><tbody class="divide-y divide-dark-700">@forelse($expenseCategories as $expenseCategory)<tr><td class="px-6 py-4 text-sm text-white font-medium">{{ $expenseCategory->name }}</td><td class="px-6 py-4 text-sm text-gray-400">{{ $expenseCategory->description ?: '—' }}</td><td class="px-6 py-4 text-xs {{ $expenseCategory->is_active ? 'text-emerald-400' : 'text-gray-500' }}">{{ $expenseCategory->is_active ? 'Active' : 'Inactive' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('admin.indus-gas.settings.expense-categories.edit', $expenseCategory) }}" wire:navigate class="text-primary-light text-sm">Edit</a><x-admin.confirm-button title="Delete expense category?" text="Delete only if it has not been used in an expense." action="$wire.delete({{ $expenseCategory->id }})" confirm-text="Delete" class="text-red-400 text-sm ml-3">Delete</x-admin.confirm-button></td></tr>@empty<tr><td colspan="4" class="px-6 py-12 text-center text-gray-500">No expense categories yet. Add categories such as Pickup Fuel, Salary, Rent, and Toll Tax.</td></tr>@endforelse</tbody></table></div></div>
