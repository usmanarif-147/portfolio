<div><div class="flex items-center gap-2 text-xs text-gray-500 mb-2"><a href="{{ route('admin.dashboard') }}" wire:navigate>Dashboard</a><span>›</span><span>Indus Gas</span><span>›</span><span class="text-gray-300">Daily Operations</span></div><div class="flex flex-col sm:flex-row justify-between gap-4 mb-8"><div><h1 class="text-2xl font-mono font-bold text-white uppercase">Daily Operations</h1><p class="text-gray-500 mt-1">Record BBN refills and customer deliveries. Stock updates automatically.</p></div><div class="flex gap-3"><a href="{{ route('admin.indus-gas.refills.create') }}" wire:navigate class="bg-primary text-white rounded-lg px-4 py-2.5 text-sm">Record BBN Refill</a><a href="{{ route('admin.indus-gas.deliveries.create') }}" wire:navigate class="bg-dark-700 text-white rounded-lg px-4 py-2.5 text-sm">Record Delivery</a></div></div>@if(session('success'))<div class="mb-5 bg-emerald-500/10 border border-emerald-500/30 text-emerald-300 rounded-lg px-4 py-3 text-sm">{{ session('success') }}</div>@endif<div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">@foreach($stock as $row)<div class="bg-dark-800 border border-dark-700 rounded-xl p-5"><p class="text-gray-400 text-sm">{{ $row['type']->name }}</p><div class="grid grid-cols-3 gap-2 mt-4 text-center"><div><p class="text-xl text-white font-mono">{{ $row['full'] }}</p><p class="text-xs text-gray-500">Full</p></div><div><p class="text-xl text-white font-mono">{{ $row['empty'] }}</p><p class="text-xs text-gray-500">Empty</p></div><div><p class="text-xl text-primary-light font-mono">{{ $row['allocated'] }}</p><p class="text-xs text-gray-500">Allocated</p></div></div></div>@endforeach</div><div class="mt-6 text-right"><a href="{{ route('admin.indus-gas.stock') }}" wire:navigate class="text-primary-light hover:underline text-sm">View warehouse stock & adjustments →</a></div><div class="grid lg:grid-cols-2 gap-6 mt-6"><div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><div class="p-5 border-b border-dark-700"><h2 class="text-white font-mono">Recent BBN Refills</h2></div>@forelse($refills as $refill)<div class="p-4 border-b border-dark-700 text-sm flex justify-between"><div><p class="text-gray-200">{{ $refill->supplier->name }}</p><p class="text-gray-500 text-xs">{{ $refill->refill_date->format('d M Y') }} · {{ $refill->filled_kg }} kg</p></div><p class="text-primary-light">Rs. {{ number_format($refill->filled_kg * $refill->lpg_rate_per_kg) }}</p></div>@empty<p class="p-6 text-gray-500 text-sm">No refills recorded yet.</p>@endforelse</div><div class="bg-dark-800 border border-dark-700 rounded-xl overflow-hidden"><div class="p-5 border-b border-dark-700"><h2 class="text-white font-mono">Recent Deliveries</h2></div>@forelse($deliveries as $delivery)<div class="p-4 border-b border-dark-700 text-sm flex justify-between"><div><p class="text-gray-200">{{ $delivery->customer->title }}</p><p class="text-gray-500 text-xs">{{ $delivery->delivery_date->format('d M Y') }}</p></div><p class="text-primary-light">Rs. {{ number_format($delivery->sale_rate_per_kg) }}/kg</p></div>@empty<p class="p-6 text-gray-500 text-sm">No deliveries recorded yet.</p>@endforelse</div></div></div>
